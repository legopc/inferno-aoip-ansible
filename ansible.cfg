[defaults]
# Default inventory — override with: ansible-playbook -i your-inventory.yml site.yml
inventory = inventory.example.yml
host_key_checking = False
# Set this to the user on your Inferno nodes (overridden per-node in inventory)
remote_user = your_username
